# Change specs

One folder per change: `YYYY-MM-DD-<slug>/`.

- `spec.md` is the contract, written by `/brainstorming`.
- `/planning` adds `plan.md` and `tickets/` beside it.

History keeps a folder after the change ships. Nothing maintains it.
